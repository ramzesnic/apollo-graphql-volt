const path = require('path');

const baseFolder = 'src';
const dbFolder = 'db';
const configFolder = 'config';

module.exports = {
  'config': path.resolve(baseFolder, configFolder, 'db-config.json'),
  'models-path': path.resolve(baseFolder, 'models'),
  'seeders-path': path.resolve(baseFolder, dbFolder, 'seeders'),
  'migrations-path': path.resolve(baseFolder, dbFolder, 'migrations')
};
